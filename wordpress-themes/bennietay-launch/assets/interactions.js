(()=>{const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const items=document.querySelectorAll('.bt-reveal');if(reduce||!('IntersectionObserver'in window)){items.forEach(el=>el.classList.add('is-visible'))}else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});items.forEach(el=>io.observe(el))}const menu=document.querySelector('.bt-menu'),nav=document.querySelector('#bt-navigation');if(menu&&nav){const close=()=>{menu.setAttribute('aria-expanded','false');nav.classList.remove('is-open')};menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('is-open',!open)});nav.addEventListener('click',close);document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target&&!reduce){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'})}}));document.querySelectorAll('[data-bt-counter]').forEach(counter=>{const target=Number(counter.dataset.btCounter||0);const run=()=>{if(reduce){counter.textContent=String(target);return}let value=0;const tick=()=>{value=Math.min(target,value+Math.max(1,Math.ceil(target/30)));counter.textContent=String(value);if(value<target)requestAnimationFrame(tick)};tick()};if(reduce||!('IntersectionObserver'in window))run();else new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){run();e.target.closest('.bt-stat')?.classList.add('is-visible');e.target.closest('.bt-stat')?.setAttribute('data-counted','1')}}),{threshold:.5}).observe(counter)});document.querySelectorAll('[data-bt-slider]').forEach(slider=>{const track=slider.querySelector('.bt-slider-track'),slides=[...slider.querySelectorAll('.bt-slide')];if(!track||slides.length<2)return;let index=0;const render=()=>{track.style.transform='translateX(-'+(index*100)+'%)';slides.forEach((s,i)=>s.setAttribute('aria-hidden',String(i!==index)))};slider.querySelector('[data-bt-prev]')?.addEventListener('click',()=>{index=(index+slides.length-1)%slides.length;render()});slider.querySelector('[data-bt-next]')?.addEventListener('click',()=>{index=(index+1)%slides.length;render()});let startX=0;track.addEventListener('touchstart',e=>{startX=e.touches[0].clientX},{passive:true});track.addEventListener('touchend',e=>{const delta=e.changedTouches[0].clientX-startX;if(Math.abs(delta)>40){index=(index+(delta<0?1:-1)+slides.length)%slides.length;render()}},{passive:true});render()});document.querySelectorAll('.bt-lightbox').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();const dialog=document.createElement('dialog');dialog.className='bt-dialog';dialog.innerHTML='<button type="button" aria-label="Close">×</button><img alt="" src="'+link.href+'">';document.body.appendChild(dialog);const close=()=>{if(dialog.open)dialog.close()};dialog.querySelector('button').addEventListener('click',close);dialog.addEventListener('cancel',close);dialog.addEventListener('close',()=>dialog.remove());dialog.showModal()}))})()
