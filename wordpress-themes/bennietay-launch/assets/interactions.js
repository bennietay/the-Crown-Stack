(()=>{const items=document.querySelectorAll('.bt-reveal');if(!('IntersectionObserver'in window)){items.forEach(el=>el.classList.add('is-visible'))}else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});items.forEach(el=>io.observe(el))}const menu=document.querySelector('.bt-menu'),nav=document.querySelector('#bt-navigation');if(menu&&nav){const close=()=>{menu.setAttribute('aria-expanded','false');nav.classList.remove('is-open')};menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('is-open',!open)});nav.addEventListener('click',close);document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}document.querySelectorAll('.bt-lightbox').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();const dialog=document.createElement('dialog');dialog.className='bt-dialog';dialog.innerHTML='<button type="button" aria-label="Close">×</button><img alt="" src="'+link.href+'">';document.body.appendChild(dialog);const close=()=>{if(dialog.open)dialog.close()};dialog.querySelector('button').addEventListener('click',close);dialog.addEventListener('cancel',close);dialog.addEventListener('close',()=>dialog.remove());dialog.showModal()}))})()
