import { useEffect, useState } from "react";
import { ArrowRight, CheckCircle2, Loader2, MessageCircle } from "lucide-react";

type Catalog = { plans: Array<any>; templates: Array<any> };

export function WaasSales() {
  const [catalog, setCatalog] = useState<Catalog>({ plans: [], templates: [] });
  const [form, setForm] = useState({ name: "", email: "", phone: "", company: "", product: "business", message: "" });
  const [state, setState] = useState<"idle" | "saving" | "done" | "error">("idle");
  useEffect(() => { fetch("/api/integrations/waas/catalog").then(r => r.ok ? r.json() : Promise.reject()).then(setCatalog).catch(() => undefined); }, []);
  const submit = async (event: React.FormEvent) => {
    event.preventDefault(); setState("saving");
    try {
      const response = await fetch("/api/capture", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.name, email: form.email, phone: form.phone, company: form.company, service: form.product === "launch" ? "Launch Website" : "Business Website", message: form.message, budget: "WAAS enquiry", timing: "ASAP", consent: true, country: "", website: "", source: "waas-sales" }) });
      if (!response.ok) throw new Error();
      setState("done");
    } catch { setState("error"); }
  };
  return <main className="min-h-screen bg-slate-950 text-white"><header className="border-b border-white/10"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-500 font-black">B</div><div><strong>BennieTay Websites</strong><p className="text-xs text-slate-400">Managed WordPress, without the maintenance burden.</p></div></div><a href="https://wa.me/60172069682" target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-lg bg-emerald-500 px-3 py-2 text-sm font-semibold"><MessageCircle className="h-4 w-4" /> WhatsApp</a></div></header><section className="mx-auto max-w-6xl px-5 py-16 lg:grid lg:grid-cols-[1.1fr_.9fr] lg:gap-16 lg:py-24"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-indigo-300">Website as a service</p><h1 className="mt-4 text-5xl font-black tracking-tight sm:text-6xl">A professional website that keeps working for your business.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-300">Launch quickly, capture enquiries, and keep your site updated with a managed plan. No plugin chasing. No surprise rebuilds.</p><div className="mt-8 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-white/10 bg-white/5 p-4"><strong>Fast launch</strong><p className="mt-1 text-sm text-slate-400">Conversion-ready pages.</p></div><div className="rounded-xl border border-white/10 bg-white/5 p-4"><strong>Managed care</strong><p className="mt-1 text-sm text-slate-400">Updates and support.</p></div><div className="rounded-xl border border-white/10 bg-white/5 p-4"><strong>Lead capture</strong><p className="mt-1 text-sm text-slate-400">Every enquiry routed.</p></div></div></div><div className="mt-12 rounded-2xl bg-white p-6 text-slate-950 shadow-2xl lg:mt-0"><h2 className="text-2xl font-bold">Get your recommendation</h2><p className="mt-2 text-sm text-slate-600">Tell us what you need. We will reply with the best-fit plan and next step.</p>{state === "done" ? <div className="mt-8 space-y-4 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-emerald-600" /><h3 className="text-xl font-bold">Request received</h3><p className="text-sm text-slate-600">We will contact you within one business day.</p><a href="https://wa.me/60172069682" target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-lg bg-emerald-600 px-4 py-3 font-semibold text-white"><MessageCircle className="h-4 w-4" /> Continue on WhatsApp</a></div> : <form onSubmit={submit} className="mt-6 space-y-4"><input required placeholder="Your name" className="w-full rounded-lg border px-3 py-3" value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /><input required type="email" placeholder="Email" className="w-full rounded-lg border px-3 py-3" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} /><input required placeholder="WhatsApp / phone" className="w-full rounded-lg border px-3 py-3" value={form.phone} onChange={e => setForm({ ...form, phone: e.target.value })} /><input placeholder="Business name" className="w-full rounded-lg border px-3 py-3" value={form.company} onChange={e => setForm({ ...form, company: e.target.value })} /><select className="w-full rounded-lg border px-3 py-3" value={form.product} onChange={e => setForm({ ...form, product: e.target.value })}><option value="business">Business Website</option><option value="launch">Launch Website</option></select><textarea required placeholder="What should the website help you achieve?" className="min-h-24 w-full rounded-lg border px-3 py-3" value={form.message} onChange={e => setForm({ ...form, message: e.target.value })} />{state === "error" && <p className="text-sm text-red-700">We could not save this request. Please WhatsApp us directly.</p>}<button disabled={state === "saving"} className="inline-flex w-full items-center justify-center rounded-lg bg-indigo-600 px-4 py-3 font-semibold text-white disabled:opacity-60">{state === "saving" ? <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Sending…</> : <>Get my recommendation <ArrowRight className="ml-2 h-4 w-4" /></>}</button></form>}</div></section><section className="border-t border-white/10 bg-white/5"><div className="mx-auto max-w-6xl px-5 py-12"><h2 className="text-2xl font-bold">Plans and templates</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{catalog.plans.length ? catalog.plans.map(plan => <div key={plan.id} className="rounded-xl border border-white/10 p-5"><h3 className="font-bold">{plan.name}</h3><p className="mt-2 text-sm text-slate-400">{plan.currency} {Number(plan.setupFee || 0).toLocaleString()} setup · {Number(plan.recurringFee || 0).toLocaleString()}/{plan.billingInterval || "month"}</p></div>) : <p className="text-sm text-slate-400">Plans are being finalised. Request a recommendation and we will quote the current offer.</p>}</div></div></section></main>;
}
